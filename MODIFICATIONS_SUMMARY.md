# Streamlit 面板修改总结

## ✅ 已完成的修改

### 1. 动态坐标轴（padding=0.1）
**修改位置**：
- 新增 `get_dynamic_range()` 函数
- 修改 `bar_chart_with_error()` 添加 `dynamic_axis` 参数
- 应用到：
  - 模型平均得分柱状图（Y轴动态）
  - 得分 vs 错误严重性散点图（X、Y轴均动态）

**效果**：坐标轴范围根据数据自动调整，上下各留 10% 空间

---

### 2. 六维度错误频次热力图（黄绿配色）
**修改位置**：
- 新增 `build_dimension_frequency()` 函数
- 新增图表：六维度错误频次热力图（模型×维度）
- 删除：模型六维度错误雷达图（与热力图重复）

**效果**：
- 左侧：平均严重度热力图（颜色：绿→黄→橙→红）
- 右侧：错误频次热力图（颜色：浅黄→深绿）

---

### 3. 题目分类雷达图（方案A）
**修改位置**：
- 新增 `QUESTION_CATEGORIES` 配置
- 新增 `get_question_category()` 函数
- 新增 `build_category_scores()` 函数
- 新增图表：模型在各类题目上的得分雷达图

**题目分类**：
```
现况解析: q1, q2, q3, q5, q6, q7
涨跌预测: q4, q8
投资建议: q9
```

**效果**：角度=类别（现况解析/涨跌预测/投资建议），每个模型一条线

---

### 4. 维度-题目热力图位置调整
**修改位置**：
- 将维度-题目热力图从"展开原始数据"中移回主面板
- 标题更新：移至此处以节省主面板空间

**效果**：仍然可查看，但默认隐藏在折叠面板中

---

## 📊 修改后的面板结构

```
RIA 评测数据可视化 (v2)
├── 数据概览（4个指标卡片）
├── 用户情况统计（3个饼图）
├── 模型平均得分（柱状图+误差线，动态Y轴）✨
├── 六维度错误平均严重度热力图（模型×维度）
├── 六维度错误频次热力图（模型×维度，黄绿配色）✨
├── 模型在各类题目上的得分雷达图（现况解析/涨跌预测/投资建议）✨
├── 得分 vs 错误严重性散点图（动态坐标轴）✨
├── 各模型错误级别分布（堆叠柱状图）
├── 模型在价值链维度的平均得分（雷达图）
├── 模型在阶段维度的平均得分（雷达图）
├── 开放反馈汇总
└── 最低分题目TOP10
```

---

## 🎨 配色方案

### 严重度热力图（平均严重度）
```
0 (无)   → #dcfce7 (浅绿)
1 (轻微) → #fef08a (黄色)
2 (明显) → #fdba74 (橙色)
3 (严重) → #ef4444 (红色)
```

### 频次热力图（错误次数）
```
0次  → 浅黄
次数多 → 深绿
```

---

## 🔧 技术细节

### 动态区间计算
```python
def get_dynamic_range(values, padding=0.1, min_val=None, max_val=None):
    range_val = max - min
    lower = math.floor(min - range_val * padding)
    upper = math.ceil(max + range_val * padding)
    return lower, upper
```

### 题目分类映射
```python
def get_question_category(qid: str) -> str:
    for category, qids in QUESTION_CATEGORIES.items():
        if str(qid) in qids:
            return category
    return "其他"
```

---

## 🚀 访问面板

**本地访问**：http://localhost:8502

**服务状态**：✅ 运行中

---

## 📝 备注

- 雷达图保持固定范围（0-10或0-3）以确保跨模型可对比性
- 散点图和柱状图使用动态范围以突出模型间差异
- 频次热力图使用 `scheme="greens"` 实现黄绿渐变